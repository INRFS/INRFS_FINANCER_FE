export const settingsService = {
  saveGeneral: async (data) => Promise.resolve({ success: true }),
  saveSMS: async (data) => Promise.resolve({ success: true }),
  saveSecurity: async (data) => Promise.resolve({ success: true }),
  saveMaintenance: async (data) => Promise.resolve({ success: true }),
};
